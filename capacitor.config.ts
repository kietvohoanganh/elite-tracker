import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ryuukyou.elitetracker',
  appName: 'Elite Tracker',
  webDir: 'dist',
};

export default config;